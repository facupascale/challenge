import { StyleSheet } from 'react-native'

const styles = StyleSheet.create({
  container: {
    alignContent: 'center',
    justifyContent: 'center',
    flex: 1,
  },
  title: {
    color: 'black',
    width: '100%',
    textAlign: 'center',
    fontSize: 14,
    fontWeight: 'bold',
  },
})

export { styles }
